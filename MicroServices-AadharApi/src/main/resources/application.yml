spring:
  application:
    name: MicroServices-AadharApi
  boot:
    admin:
      client:
        url: http://localhost:4444/
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'